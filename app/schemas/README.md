# Room database schemas

Room exports schema JSON files here when the app is built. Commit those generated
files so database migrations can be validated against earlier schema versions.

The schema directory is configured by the Room Gradle plugin in `app/build.gradle.kts`.
